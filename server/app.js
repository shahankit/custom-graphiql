import React from 'react';
import ReactDOM from 'react-dom';
import CustomGraphiQL from '../src/index';

const rootElement = document.getElementById('root');

ReactDOM.render(
  <CustomGraphiQL />,
  rootElement
);
